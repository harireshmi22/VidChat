# Friend Request Box

Like 2 user User A send the request to the user B, so how User B check the request from any where, so make a box for that all the frient request save there.

## Invite friend to call

Make a button on the user profile

1. Take a call and join it, (take it as a user A) create a random room ID and send it to the User B,
2. User B join the room and the call start,
3. Now in User A screen user B name should be displayed, and vice versa.

## Make a profile for the Users

User has UserId, username, profile picture, and they can update it from the profile settings.

## Random colors for profile picture

Generate random colors for each user's profile picture based on their name.

## Auto connect User with server

when the user login in the system, user should be automatically connected with the server.

## Create a Chatting room for Users direct Message

like a whatsapp chat, where user can message each other, and also create a group chat with multiple users, and also in real time.

# Problem 1: User didn't get the incoming video call request

I have sent thhe request to the user B, User A sent video Calling request and How the user B get the incoming video calling from the user A, also write the code for accepting the request and declining the request, accepting the request should add that user to the friends list of user B, and declining the request should remove that user from the pending requests of user B

## Problem 2: How the User get the friend request

User A sent the friend request to the user B, But User B didn't get any friend request from any user, so how User B accept the request coming from any user, fix that too, and also write the code for accepting the request, and declining the request, accepting the request should add that user to the friends list of user B, and declining the request should remove that user from the pending requests of user B
